server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/km0dev
    username: admin
    password: pB55v1Ks?AxB
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  security:
    oauth2:
      client:
        registration:
          strava:
            client-id: 179949
            client-secret: c60e34f786519935d4da649516af780ecec5b48a
            scope: activity:read_all
            token-name: access_token
            authentication-scheme: query
            client-authentication-scheme: form
            client-authentication-method: client_secret_post
            client-name: Strava
            redirect-uri: "http://localhost:8080/exchange_token"
            authorization-grant-type: authorization_code
        provider:
          strava:
            authorization-uri: https://www.strava.com/oauth/authorize
            token-uri: https://www.strava.com/oauth/token
            user-info-uri: https://www.strava.com/api/v3/athlete
            user-name-attribute: id